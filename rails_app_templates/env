# Unicorn process count
UNICORN_COUNT=1
UNICORN_TIMEOUT=30

APP_HOST=local.<%= application_name %>.com
API_SUBDOMAIN=local.api
CORS_ORIGINS=local.<%= application_name %>.com

# Rails secret, as accessed through
# the secrets.yml file.
SECRET_KEY_BASE=<%= generate_secret %>

DEVISE_SECRET=<%= generate_secret %>
DEVISE_MAILER_SENDER=noreply@<%= application_name %>.com
DEVISE_PEPPER=<%= generate_secret %>
